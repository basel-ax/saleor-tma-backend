Phase 6 — Deployment Scaffolding

Goals
- Provide Cloudflare Workers deployment scaffolding and local dev/test workflow.
- Include Wrangler configuration hints and environment variable mapping.

Deliverables
- Deployment guide snippet for wrangler.toml setup (JS/TS target).
- Local dev run commands (wrangler dev) and test harness integration (SPEC_KIT_BASE_URL).
- Environment variable map: SALEOR_API_URL, SALEOR_TOKEN, BACKEND_BASE_URL, TELEGRAM_BOT_TOKEN, etc.

(End of Phase 6)
